name: bmc_audio_windows_example
description: "Demonstrates how to use the bmc_audio_windows plugin."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^6.1.1
  ffi: ^2.1.3
  bmc_audio_windows:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
