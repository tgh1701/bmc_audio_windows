# Managed by Flutter - do not edit manually.
cmake_minimum_required(VERSION 3.14)

set(PROJECT_NAME "bmc_audio_windows")
project(${PROJECT_NAME} LANGUAGES C)

# FFI plugin — native code is in src/
# The ffiPlugin: true in pubspec.yaml tells Flutter to look for src/CMakeLists.txt
